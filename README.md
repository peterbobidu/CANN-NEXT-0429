# CANN Figma 复刻页面

已按 Figma 节点 `903:34` 实现为 React + TypeScript + Tailwind 页面。

## 运行

```bash
npm install
npm run dev
```

然后访问 Vite 输出的本地地址。

## 说明

- 页面主体尺寸按 Figma 画布 `1920 × 2562` 复刻，并用整体缩放适配较小屏幕。
- 图片和图标使用 Figma MCP 返回的原始资产 URL，避免自行替换素材。
- 当前项目已完成依赖安装，并通过 `npm run build` 构建检查。
